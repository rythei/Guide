# GuideVectorModel

This repository takes in an image with a path and creates a vector field. The main algorithm is in the create_field.py file.
Running main.py runs the algorithm on an image that is cleaned from the clean_image.py script.
